# Dispatcher for the jyliutestci end-to-end test branch.
# Routes to liujinye-sys/public-actions test branch carrying the
# vendor-based matrix + result aggregation fix.

name: CI

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

jobs:
  ci:
    uses: liujinye-sys/public-actions/.github/workflows/ci-real.yml@test/ci-matrix-by-vendor
    secrets: inherit
